server:
  port: 8963
spring:
  application:
    name: manager-service
management:
  server:
    port: 8964
  endpoints:
    web:
      exposure:
        include: '*'
feign:
  hystrix:
    enabled: false